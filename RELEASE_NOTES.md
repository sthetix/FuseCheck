## v1.0.2

- Fixed firmware version showing as `1.0.0` when NCA detection fails
- When firmware is not found in the database, display now shows `N/A` instead of a misleading version
- Added `STATUS: FIRMWARE NOT DETECTED` state with a prompt to check for an updated build at github.com/sthetix/FuseCheck
- Required Fuses also shows `N/A` when firmware cannot be detected
